/**
 * TS engine americké dámy – samostatný proces za JSON Lines protokolem.
 * Zatím jen kostra balíčku, protokol a search přijdou v M3.
 */

/** Identifikátor enginu pro protokolovou zprávu hello. */
export const ENGINE_ID = 'checkers-ts-engine';
